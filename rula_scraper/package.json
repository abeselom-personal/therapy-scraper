{
  "name": "rula-scraper",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "scrape-basic": "node scrape_basic.js",
    "scrape-detail": "node scrape_detail.js",
    "export": "node export_to_excel.js",
    "install-playwright": "npx playwright install firefox",
    "docker-build": "docker build -t rula-scraper .",
    "docker-run": "docker run -it --rm rula-scraper"
  },
  "dependencies": {
    "playwright": "^1.56.0",
    "mongodb": "^5.8.0",
    "exceljs": "^4.4.0"
  }
}
